//this file is needed because the sweetalert typings need to be overwritten because they are garbage
//the real fix here is to stop using sweetalert
import swal, { SweetAlert } from 'sweetAlert/typings/core';


declare global {
  //const swal: SweetAlert;
  const sweetAlert: SweetAlert;
}

export default swal;
export as namespace swal;
